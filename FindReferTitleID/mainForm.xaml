<UserControl x:Class="FindReferTitleID.mainForm"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:FindReferTitleID"
             mc:Ignorable="d"
             FontSize="14"
             MaxHeight="300"
             MinHeight="300"
             MaxWidth="270"
             MinWidth="270"
             Background="#3B4453">
    <!--Width="130"
             Height="200"sfs-->
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="Padding"
                    Value="10" />
            <Setter  Property="BorderBrush"
                     Value="Black" />
            <Setter Property="BorderThickness"
                    Value="0 0 4 4" />
            <Setter Property="Cursor"
                    Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}"
                                              VerticalAlignment="{TemplateBinding VerticalContentAlignment}" />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="MediumSpringGreen" />
                            </Trigger>
                            <Trigger Property="IsPressed"
                                     Value="True">
                                <Setter Property="Background"
                                        Value="DarkOliveGreen" />
                                <Setter Property="Foreground"
                                        Value="Black" />
                            </Trigger>
                            <Trigger Property="IsEnabled"
                                     Value="False">
                                <Setter Property="Opacity"
                                        Value="0.5" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        <Button Grid.Row="0"
                HorizontalAlignment="Stretch"
                Content="TitleID-Sheet"
                Height="50"
                Margin="5,10,5,0"
                Background="Green"
                x:Name="btnTileIDSheet"
                Click="btnbtnTileIDSheet_Click" />
        <Button  Grid.Row="1"
                 HorizontalAlignment="Stretch"
                 Content="Section-TitleID"
                 Height="50"
                 Margin="5,10,5,0"
                 Background="Red"
                 x:Name="btnSectionTileID"
                 Click="btnSectionTileID_Click" />
        <Button  Grid.Row="2"
                 HorizontalAlignment="Stretch"
                 Content="Find TitleID"
                 Height="50"
                 Margin="5,10,5,0"
                 Background="Yellow"
                 Foreground="Black"
                 x:Name="btnFindTitleID"
                 Click="btnbtnFindTitleID_Click" />
        <Button  Grid.Row="3"
                 HorizontalAlignment="Stretch"
                 Content="Callout-Tile ID1"
                 FontWeight="Bold"
                 Height="50"
                 Margin="5,10,5,0"
                 Background="Orange"
                 Foreground="Black"
                 x:Name="btnRefeCallout"
                 Click="btnReferCallout_Click" />
    </Grid>
</UserControl>
